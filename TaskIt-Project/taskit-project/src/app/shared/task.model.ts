export class Task {
  constructor( public title: string, public details: string, public dueDate: string, public priority: string, public status: string){}
  }

 /*  export interface Task {
    name: string;
    dueDate: Date;
    priority: string;
    status: string;
    id?: string;
  } */
